name: JDK StackWalker upcall repro (throwaway)

# Standalone diagnostic for a suspected JDK 25 bug: java.lang.StackWalker
# crashing/hanging the JVM when invoked from a thread the JVM did not
# create, attached via a foreign-function upcall. Not part of the project's
# normal CI -- self-contained (the reproducer source is inlined below, not
# read from the working tree). See apache/maven-surefire#3459.
#
# Triggers on push to this branch only -- workflow_dispatch requires the
# workflow file to be on the default branch first, which this deliberately
# avoids. Delete this workflow file (and the branch) once it has run and
# its output has been captured for the JDK bug report.

on:
  push:
    branches:
      - jdk-stackwalker-repro

jobs:
  repro:
    name: run on windows-latest
    runs-on: windows-latest
    permissions: {}
    steps:
      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          distribution: zulu
          java-version: "25"

      - name: Write reproducer
        shell: bash
        run: |
          cat > LoggerUpcallRepro.java <<'JAVA'
          import java.lang.foreign.Arena;
          import java.lang.foreign.FunctionDescriptor;
          import java.lang.foreign.Linker;
          import java.lang.foreign.MemorySegment;
          import java.lang.foreign.SymbolLookup;
          import java.lang.invoke.MethodHandle;
          import java.lang.invoke.MethodHandles;
          import java.lang.invoke.MethodType;

          import static java.lang.foreign.ValueLayout.ADDRESS;
          import static java.lang.foreign.ValueLayout.JAVA_INT;
          import static java.lang.foreign.ValueLayout.JAVA_LONG;

          /**
           * Targeted reproducer: does {@code System.getLogger(name).log(Level.ERROR, msg, throwable)} -- exactly what
           * this project's own upcall dispatch methods do in their mandatory "must not throw across the upcall
           * boundary" catch-all (see {@code CompactionFilter.filterDispatch}, {@code
           * CompactionFilterFactory.createFilterDispatch}, {@code EventNotifierBridge}'s dispatch methods) -- crash the
           * JVM when called from a thread the JVM did not create, attached via a foreign-function upcall.
           *
           * <p>{@code jdk.internal.logger.SimpleConsoleLogger} (the default backend for {@code System.getLogger(...)}
           * unless {@code java.util.logging} is separately configured) calls {@code
           * StackWalker.getInstance(RETAIN_CLASS_REFERENCE).walk(...)} on every {@code log(...)} call, unconditionally,
           * to infer the caller for the printed line -- see {@code SimpleConsoleLogger.getCallerInfo()} /
           * {@code CallerFinder}. Two earlier attempts calling {@code StackWalker} directly (with the default option
           * set, and via reflection mirroring Surefire's own call shape) did not reproduce the bug; this one uses
           * {@code RETAIN_CLASS_REFERENCE} and the real predicate-driven frame walk JDK logging actually performs,
           * exactly matching this project's real, non-mutated, non-PIT code path.
           *
           * <p>Run on Windows with: {@code java --enable-native-access=ALL-UNNAMED LoggerUpcallRepro.java}
           */
          public class LoggerUpcallRepro {

          	private static final Linker LINKER = Linker.nativeLinker();
          	private static final Arena ARENA = Arena.global();
          	private static final System.Logger LOG = System.getLogger(LoggerUpcallRepro.class.getName());

          	// DWORD WINAPI ThreadProc(LPVOID lpParameter)
          	private static final FunctionDescriptor THREAD_PROC_DESC = FunctionDescriptor.of(JAVA_INT, ADDRESS);

          	// HANDLE CreateThread(LPSECURITY_ATTRIBUTES, SIZE_T, LPTHREAD_START_ROUTINE, LPVOID, DWORD, LPDWORD)
          	private static final MethodHandle MH_CREATE_THREAD;

          	// DWORD WaitForSingleObject(HANDLE hHandle, DWORD dwMilliseconds)
          	private static final MethodHandle MH_WAIT_FOR_SINGLE_OBJECT;

          	private static final int WAIT_TIMEOUT = 0x102;
          	private static final int WAIT_MS = 30_000;
          	private static final int ITERATIONS = 200;

          	private static volatile boolean crashedOrHung = true;

          	static {
          		if (!System.getProperty("os.name", "").toLowerCase().contains("win")) {
          			throw new IllegalStateException("this reproducer only exercises the Windows-only bug; run it on Windows");
          		}
          		SymbolLookup kernel32 = SymbolLookup.libraryLookup("kernel32.dll", ARENA);
          		MH_CREATE_THREAD = LINKER.downcallHandle(
          				kernel32.find("CreateThread").orElseThrow(),
          				FunctionDescriptor.of(ADDRESS, ADDRESS, JAVA_LONG, ADDRESS, ADDRESS, JAVA_INT, ADDRESS));
          		MH_WAIT_FOR_SINGLE_OBJECT = LINKER.downcallHandle(
          				kernel32.find("WaitForSingleObject").orElseThrow(),
          				FunctionDescriptor.of(JAVA_INT, ADDRESS, JAVA_INT));
          	}

          	public static void main(String[] args) throws Throwable {
          		System.out.println("PID = " + ProcessHandle.current().pid());

          		MethodHandles.Lookup lookup = MethodHandles.lookup();
          		MemorySegment threadEntry = LINKER.upcallStub(
          				lookup.findStatic(LoggerUpcallRepro.class, "threadMain", MethodType.methodType(int.class, MemorySegment.class)),
          				THREAD_PROC_DESC, ARENA);

          		MemorySegment thread = (MemorySegment) MH_CREATE_THREAD.invokeExact(
          				MemorySegment.NULL, 0L, threadEntry, MemorySegment.NULL, 0, MemorySegment.NULL);
          		if (MemorySegment.NULL.equals(thread)) {
          			throw new IllegalStateException("CreateThread failed");
          		}

          		System.out.println("main: waiting up to " + WAIT_MS + "ms for the foreign thread to finish "
          				+ ITERATIONS + " LOG.log iterations...");
          		int result = (int) MH_WAIT_FOR_SINGLE_OBJECT.invokeExact(thread, WAIT_MS);
          		if (result == WAIT_TIMEOUT) {
          			System.out.println("HANG: WaitForSingleObject timed out -- the foreign thread never returned"
          					+ " from LOG.log(). This matches the indefinite-hang shape of the surefire bug.");
          			System.exit(2);
          		}

          		System.out.println(crashedOrHung
          				? "FAIL: process reached the end of main() without threadMain ever reporting completion"
          				: "PASS: all " + ITERATIONS + " LOG.log iterations completed normally on the foreign thread");
          	}

          	/**
          	 * Entry point for the OS thread {@code CreateThread} spawns -- the JVM has never seen this thread
          	 * before. Mirrors this project's own upcall dispatch catch-all: an upcall runs, something throws, the
          	 * catch block logs it via {@code System.Logger}, all on RocksDB's (here, simulated) background thread.
          	 */
          	private static int threadMain(MemorySegment lpParameter) {
          		System.out.println("threadMain: running on a JVM-attached (not JVM-created) thread");
          		try {
          			for (int i = 0; i < ITERATIONS; i++) {
          				try {
          					throw new RuntimeException("boom");
          				} catch (Throwable t) {
          					// exactly CompactionFilter.filterDispatch's catch-all shape
          					LOG.log(System.Logger.Level.ERROR, "filter callback failed", t);
          				}
          				if (i % 20 == 0) {
          					System.out.println("threadMain: completed iteration " + i + "/" + ITERATIONS);
          				}
          			}
          		} catch (Throwable t) {
          			t.printStackTrace();
          			return 1;
          		}

          		System.out.println("threadMain: all iterations returned normally");
          		crashedOrHung = false;
          		return 0;
          	}
          }
          JAVA

      - name: Run reproducer (bounded -- must not let a hang wedge the runner)
        shell: bash
        timeout-minutes: 5
        run: java --enable-native-access=ALL-UNNAMED LoggerUpcallRepro.java

      - name: Capture crash artifacts on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: jdk-crash-artifacts
          path: |
            hs_err_pid*.log
            replay_pid*.log
          if-no-files-found: ignore
